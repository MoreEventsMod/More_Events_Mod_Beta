decision_mem_xaal_activate_the_engine = {
	icon = decision_surreal_visions
	resources = {
		category = decisions
		cost = {
			influence = 100
		}
	}

	potential = {
		from = {
			has_modifier = mem_xaal_engine_edict_enabled
		}
		exists = space_owner
		space_owner = {
			is_same_value = from
		}
		is_colonizable = yes
		num_pops = 0
		NOR = {
			exists = owner
			habitable_structure = yes
			is_ringworld = yes
		}
	}

	allow = {
		custom_tooltip = {
			fail_text = decision_mem_xaal_activate_the_engine_rich_history
			NOR = {
				is_planet_class = pc_city
				is_planet_class = pc_relic
				is_planet_class = pc_hive
				is_planet_class = pc_machine
				is_planet_class = pc_nuked
				exists = archaeological_site
			}
		}
		custom_tooltip = {
			fail_text = decision_mem_xaal_already_applied
			NOT = { has_planet_flag = mem_xaal_planet_had_engine_applied }
		}
	}

	effect = {
		custom_tooltip = decision_mem_xaal_activate_the_engine_effect
		set_planet_flag = mem_xaal_planet_had_engine_applied
		hidden_effect = {
			from = {
				remove_modifier = mem_xaal_engine_edict_enabled
			}
		}
	}

	ai_weight = {
		weight = 0
	}
}