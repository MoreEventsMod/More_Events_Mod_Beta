#Asteroid Computer
#Idea and writing by shockedsystem
#Original Code by asnys
#Code adapted by Traditum
anomaly = {
	event = mem_antarans.202
	category = mem_antarans_category
	
	weight = 1
	
	potential = {
		owner = {
			NOT = {
				has_country_flag = mem_antaran_destroyed_colony_a_found
			}
		}
	}
}

anomaly = {
	event = mem_antarans.203
	category = mem_antarans_category
	
	weight = 1
	
	potential = {
		owner = {
			NOT = {
				has_country_flag = mem_antaran_destroyed_colony_b_found
			}
		}
	}
}

anomaly = {
	event = mem_antarans.204
	category = mem_antarans_category
	
	weight = 1
	
	potential = {
		owner = {
			NOT = {
				has_country_flag = mem_antaran_destroyed_colony_c_found
			}
		}
	}
}

anomaly = {
	event = mem_antarans.205
	category = mem_antarans_category
	
	weight = 1
	
	potential = {
		owner = {
			NOT = {
				has_country_flag = mem_antaran_destroyed_colony_d_found
			}
		}
	}
}

anomaly = {
	event = mem_antarans.206
	category = mem_antarans_category
	
	weight = 1
	
	potential = {
		owner = {
			has_country_flag = mem_antaran_destroyed_colony_a_found
			has_country_flag = mem_antaran_destroyed_colony_b_found
			has_country_flag = mem_antaran_destroyed_colony_c_found
			has_country_flag = mem_antaran_destroyed_colony_d_found
		}
	}
}
