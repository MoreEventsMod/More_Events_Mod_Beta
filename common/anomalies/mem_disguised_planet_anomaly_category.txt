#Abandoned Mecha
#Idea and writing by ForeDaMole
#Code by LamilLerran

anomaly_category = {
	key = mem_disguised_planet_category
	desc = mem_disguised_planet_category_desc
	picture = GFX_evt_toxic
	level = 3
	
	spawn_chance = {
		modifier = {
			is_planet_class = pc_toxic
			planet_size > 12
			from.owner = {
				NOT = {
					has_country_flag = mem_disguised_planet_spawned
				}
			}
			add = 3
		}
	}
	
	on_spawn = {
		from.owner = {
			set_country_flag = mem_disguised_planet_spawned
		}
	}
	
	on_fail = {
		ship_event = {
			id = mem_disguised_planet.100
		}
	}

	on_critical_fail = {
	}
}
