@base_weight = 1
@biased_weight = 3

# Unweighted
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category

	weight = @base_weight

	potential = {
		always = yes
	}
}

# Bias towards mineral salvage
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category.materials_bias

	weight = @biased_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category.materials_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category.materials_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category.materials_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category.materials_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category.materials_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

# Bias towards power generation
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category.industry_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category.industry_bias

	weight = @biased_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category.industry_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category.industry_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category.industry_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category.industry_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

# Bias towards engineering
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category.voidcraft_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category.voidcraft_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category.voidcraft_bias

	weight = @biased_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category.voidcraft_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category.voidcraft_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category.voidcraft_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

# Bias towards small warp loop
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category.field_manipulation_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category.field_manipulation_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category.field_manipulation_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category.field_manipulation_bias

	weight = @biased_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category.field_manipulation_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category.field_manipulation_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

# Bias towards Strange warp technology
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category.particles_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category.particles_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category.particles_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category.particles_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category.particles_bias

	weight = @biased_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category.particles_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

# Bias towards Interesting biology
anomaly = {
	event = mem_asteroid_derelict_structure.2
	category = mem_asteroid_derelict_structure_category.biology_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.3
	category = mem_asteroid_derelict_structure_category.biology_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.4
	category = mem_asteroid_derelict_structure_category.biology_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.5
	category = mem_asteroid_derelict_structure_category.biology_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.6
	category = mem_asteroid_derelict_structure_category.biology_bias

	weight = @base_weight

	potential = {
		always = yes
	}
}

anomaly = {
	event = mem_asteroid_derelict_structure.7
	category = mem_asteroid_derelict_structure_category.biology_bias

	weight = @biased_weight

	potential = {
		always = yes
	}
}
