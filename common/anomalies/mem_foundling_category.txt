#Foundling
#Idea and writing by AbsoluteVacuum
#Code by ViolentBeetle

anomaly_category = {
	key = mem_foundling_category
	desc = mem_foundling_category_desc
	picture = GFX_evt_in_the_dark
	level = 1
	
	spawn_chance = {
		modifier = {
			OR = {
				is_planet_class = pc_barren
				is_planet_class = pc_barren_cold
			}
			from.owner = {
				NOR = {
					has_country_flag = mem_foundling
					has_ethic = ethic_gestalt_consciousness
				}
			}
			add = 1
		}
	}
	
	on_spawn = {
		from.owner = {
			set_country_flag = mem_foundling
		}
	}
	
	on_fail = {
		ship_event = {
			id = mem_foundling.99
		}
	}

	# on_critical_fail = {
	# }
}
