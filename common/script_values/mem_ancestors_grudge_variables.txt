mem_ancestors_grudge_situation_speed = {
	base = -100
	#add = modifier:mem_ancestors_grudge_artifact_power_produces_mult
	mult = modifier:mem_ancestors_grudge_artifact_power_produces_mult
}

mem_ancestors_grudge_situation_speed_runes = {
	base = 100
	#add = modifier:mem_ancestors_grudge_artifact_power_produces_mult
	mult = modifier:mem_ancestors_grudge_rune_power_produces_mult
}

# on the relic forge, makes smithing fater per 
mem_ancestors_grudge_smithing_speed_per_jobs = {
	base = 0
	complex_trigger_modifier = {
		trigger = num_assigned_jobs
		parameters = {			
			job = mem_ancestors_grudge_relicsmith		
		}
		mode = add		# Scale with alloyer jobs
	}
	complex_trigger_modifier = {
		trigger = num_assigned_jobs
		parameters = {			
			job = mem_ancestors_grudge_relicsmith_drone		
		}
		mode = add		# Scale with alloyer jobs
	}
	complex_trigger_modifier = {
		trigger = num_assigned_jobs
		parameters = {			
			job = mem_ancestors_grudge_runesmith		
		}
		mode = add		# Scale with alloyer jobs
	}
	complex_trigger_modifier = {
		trigger = num_assigned_jobs
		parameters = {			
			job = mem_ancestors_grudge_runesmith_drone			
		}
		mode = add		# Scale with alloyer jobs
	}
	divide = 500	# Renormalise to "per 100 jobs"
	add = 0.1		# so the TT doesnt look ugly
}


# pop number but /10
mem_ancestors_grudge_situation_pop_count = {
	base = 0
	add = trigger:pop_amount
	mult = 0.001
}

# negativ growth
mem_ancestors_grudge_factory_pop_count = {
	base = 0
	add = trigger:pop_amount
	max = 90
}

# overflow
mem_ancestors_grudge_hoarder_overflow = {
	base = 1
	complex_trigger_modifier = {
		trigger = resource_revenue_compare
		parameters = {
			resource = minor_artifacts
		}
		mode = mult
	}
	#max = 4
}

# relics
mem_ancestors_grudge_hoarder_building = {
	base = 1
	add = trigger:num_owned_relics
	#max = 4
}

# extra forge jobs
mem_ancestors_grudge_relicforge_jobs = {
	base = 200
	add = this.mem_ancestors_grudge_relicforge_extrajobs
	#max = 4
}


mem_smith_job_number = {
	base = 1
	mult = modifier:mem_ancestors_grudge_progress_add
	# complex_trigger_modifier = {
	# 	trigger = pop_count
	# 	parameters = {
	# 		limit = {
	# 			OR = {
	# 				has_job = mem_ancestors_grudge_relicsmith
	# 				has_job = mem_ancestors_grudge_relicsmith_drone
	# 				has_job = mem_ancestors_grudge_runesmith
	# 				has_job = mem_ancestors_grudge_runesmith_drone
	# 			}
	# 		}
	# 	}
	# 	mult = 0.03
	# 	mode = add
	# }
	# max = 20
}

mem_ancestors_artisan_workforce = {
	#set = value:psionic_workforce_basic_value
	base = 0
	modifier = {
		add = modifier:pop_artisan_bonus_workforce_mult
	}
	modifier = {
		add = modifier:pop_bureaucrat_bonus_workforce_mult
	}
}

mem_ancestors_artisan_workforce_gestalt = {
	#set = value:psionic_workforce_basic_value
	base = 0
	modifier = {
		add = modifier:pop_artisan_drone_bonus_workforce_mult
	}
	modifier = {
		add = modifier:pop_coordinator_bonus_workforce_mult
	}
	modifier = {
		add = modifier:pop_synapse_drone_bonus_workforce_mult
	}
}