mem_broken_clock_fellow_rebels = {
    opinion = {
        base = 100

        modifier = {
            factor = 0.5
            is_xenophobe = yes
        }
        modifier = {
            factor = 0
            OR = {
                is_homicidal = yes
                is_at_war_with = FROM
                NOT = { has_country_flag = mem_broken_clock_breakaways }
            }
        }
    }
}