# add situtation progress
mem_eden_protocol_add_protocol_progress = {
	if = {
		limit = {
			any_situation = {
				is_situation_type = situation_mem_eden_protocol_protocolswill
			}
		}
		random_situation = {
			limit = {
				is_situation_type = situation_mem_eden_protocol_protocolswill
			}
			add_situation_progress = $AMOUNT$
		}
	}
	# this is sad actually^^
	else = {
		add_resource = {
			unity = 100
			mult = $AMOUNT$
		}
	}
}

# this is on top of the actual terraforming
mem_eden_protocol_on_terraformed = {
	space_owner = {
		mem_eden_protocol_add_protocol_progress = {
			AMOUNT = 25
		}
	}
	remove_planet_flag = mem_eden_protocol_terraforming_candidate
	change_pc = rl_habitable_normal
	if = {
		limit = {
			is_planet_class = pc_nuked
		}
		set_timed_planet_flag = {
			flag = mem_was_nuked
			days = 10
		}
	}
	reroll_planet = yes
	if = {
		limit = {
			has_planet_flag = mem_was_nuked
		}
		add_blocker = {
			type = d_dangerous_wildlife_blocker
		}
		add_blocker = {
			type = d_dangerous_wildlife_blocker
		}
		add_blocker = {
			type = d_dangerous_wildlife_blocker
		}
		add_blocker = {
			type = d_dangerous_wildlife_blocker
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_mountain_range
		}
		add_blocker = {
			type = d_mountain_range
		}
	}
	else = {
		add_blocker = {
			type = d_dangerous_wildlife_blocker
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_active_volcano
		}
		add_blocker = {
			type = d_mountain_range
		}
		add_blocker = {
			type = d_mountain_range
		}
		add_blocker = {
			type = d_mountain_range
		}
		add_blocker = {
			type = d_mountain_range
		}
	}
	if = {
		limit = {
			has_modifier = terraforming_candidate
		}
		remove_modifier = terraforming_candidate
	}
	if = {
		limit = {
			has_modifier = toxic_terraforming_candidate
		}
		remove_modifier = toxic_terraforming_candidate
	}
	if = {
		limit = {
			has_modifier = frozen_terraforming_candidate
		}
		remove_modifier = frozen_terraforming_candidate
	}
	solar_system = {
		random_system_planet = {
			limit = {
				is_planet_class = pc_habitat
			}
			change_variable = {
				which = mem_eden_protocol_planets_edenized
				value = 1
			}
		}
	}
	if = {
		limit = {
			NOT = {
				exists = owner
			}
		}
		start_colony = {
			owner = space_owner
		}
	}
}

# create random pre ftl on scoped planet
mem_eden_protocol_create_pre_ftl = {
	generate_very_early_pre_ftls_on_planet = yes
	last_created_country = {
		#set_country_type = primitive
		establish_communications = root.space_owner
		if = {
			limit = {
				NOT = {
					any_situation = {
						is_situation_type = pre_ftl_tech_progress_situation
					}
				}
			}
			start_situation = {
				type = pre_ftl_tech_progress_situation
			}
		}
		random_situation = {
			limit = {
				is_situation_type = pre_ftl_tech_progress_situation
			}
			set_situation_flag = tech_frozen
		}
		root = {
			set_owner = prev
		}
	}
	every_owned_pop_group = {
		limit = {
			has_trait = trait_machine_unit
		}
		kill_pop_group = {
			pop_group = this
			percentage = 100
		}
	}
	# the killed of robots
	add_blocker = {
		type = d_unplugged_assimilator_hulk
	}
	
}
