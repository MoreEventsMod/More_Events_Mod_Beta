# Establishing comms when probe stops in your system
mem_surveyor_try_to_communicate = {
    space_owner = {
        IF = {
            limit = {
                NOT = { has_country_flag = mem_surveyor_spotted_probe }
                always = no
            }
            country_event = { id = mem_surveyor.3 }
        }
        ELSE_IF = {
            limit = {
                has_country_flag = mem_surveyor_spotted_probe
                NOT = { has_country_flag = mem_surveyor_opened_comms }
            }
            random_list = {
                50 = {  }
                50 = {
                    country_event = { id = mem_surveyor.4 }
                }
            }
        }
    }
}

mem_surveyor_clear_convo_flags = {
    remove_country_flag = mem_surveyor_diplomacy_a
    remove_country_flag = mem_surveyor_diplomacy_b
    remove_country_flag = mem_surveyor_diplomacy_c
}