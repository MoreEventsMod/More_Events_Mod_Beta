@activation_cost = 150
@triumph_duration = 3600
r_mem_xaal_engine = {
    activation_duration = @triumph_duration
    portrait = GFX_relic_mem_xaal_engine
    sound = event_activating_unknown_technology

    resources = {
		category = relics
		# Activation cost
		cost = {
			influence = @activation_cost
        }
    }

    ai_weight = {
        weight = 100
    }

    triggered_country_modifier = {
		potential = {
			always = yes
		}
		ship_disengage_chance_mult = 0.25
    }
    
    score = 500 # Almost as good as great khan

    active_effect = {
		add_modifier = {
			modifier = mem_xaal_engine_active_effect
			days = @triumph_duration
		}
		custom_tooltip = mem_xaal_engine_effect_tooltip
		hidden_effect = {
			add_modifier = {
				modifier = mem_xaal_engine_edict_enabled
				days = @triumph_duration
			}
		}
		custom_tooltip = relic_triumph_cooldown
		hidden_effect = {
			add_modifier = {
				modifier = "relic_activation_cooldown" 
				days = @triumph_duration
			}		
		}
    }

    possible = {
		custom_tooltip = {
			fail_text = "requires_relic_no_cooldown"
			NOT = { has_modifier = relic_activation_cooldown }
        }
	}
}