### Overwrite

has_void_dweller_origin = {
	OR = {
		has_origin = origin_void_dwellers
		has_origin = origin_void_machines
		has_origin = origin_mem_eden_protocol
	}
}

### Overwrite

mem_eden_pre_ftl_possible_planet = {
	AND = {
		habitable_planet_not_urban = yes # without this, it stole my perfect machine world
		is_artificial = no
	}
}