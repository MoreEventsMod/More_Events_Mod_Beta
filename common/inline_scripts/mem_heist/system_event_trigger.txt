if = {
	limit = {
		from = {
			has_star_flag = $SYSTEM_FLAG$
		}
		NOT = {
			has_country_flag = $SYSTEM_FLAG$_claimed
		}
	}
	set_country_flag = $SYSTEM_FLAG$_claimed
	country_event = {
		id = $EVENT_ID$
		scopes = {
			FROM = FROM
		}
	}
}
