namespace = asnys

# This is a utility event to make the event sequence easier to test. To test, load the 
# game, open the console, and type event ancient_robots.0 in; this will trigger the sequence on
# your homeworld.

country_event = {
	id = asnys.16
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		capital_scope = {
			set_planet_flag = ancient_robot_spawn_set
			random_tile = {
				limit = {
					has_blocker = yes
				}
				remove_blocker = yes
				set_blocker = tb_mountain_range
			}
			planet_event = { id = mem_ancient_robots.201 }
		}
	}
}

country_event = {
	id = asnys.19
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		capital_scope = {
			random_pop = {
				purge = yes
			}
		}
	}
}
