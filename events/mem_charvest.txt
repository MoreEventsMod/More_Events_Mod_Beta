#Crystalline harvest experiments

namespace = mem_charvest

#Initializer
event = {
	id = mem_charvest.1
	
	hide_window = yes
	
	is_triggered_only = yes
	
	immediate = {
		create_country = {
			name = "Space Jerks"
			type = "mem_charvest"
			species = last_created
			name_list = "Prethoryn"
			flag = {
				icon= {
					category = "special"
					file = "the_swarm.dds"
				}
				background= {
					category = "backgrounds"
					file = "new_dawn.dds"
				}
				colors={
					"blue"
					"red"
					"null"
					"null"
				}
			}
		}
		last_created_country = {
			save_global_event_target_as = jerks
		}
		every_planet = {
			limit = {
				is_colonizable = yes
			}
			random_list = {
				90 = {}
				10 = {
					planet_event = {
						id = mem_charvest.2
					}
				}
			}
		}
	}
}

#Occupation
planet_event = {
	id = mem_charvest.2
	
	hide_window = yes
	
	is_triggered_only = yes
	
	immediate = {
		set_owner = event_target:jerks
	}
}