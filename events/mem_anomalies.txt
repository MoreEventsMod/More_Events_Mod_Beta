namespace = mem_anomalies
ship_event = {
	id = mem_anomalies.1
	title = "mem_anomalies.1.name"
	desc = "mem_anomalies.1.desc"
	picture = "GFX_evt_engineering_cache"
	is_triggered_only = yes
	# specimen = mem_laser_rifle
	option = {
		name = FASCINATING
		from = {
			add_deposit = d_engineering_5
		}
	}
	option = {
		name = mem_anomalies.1.b
		owner = {
			add_monthly_resource_mult = {
				resource = alloys
				value = @tier2materialreward
				min = @tier2materialmin
				max = @tier2materialmax
			}
			add_monthly_resource_mult = {
				resource = minerals
				value = @tier3materialreward
				min = @tier3materialmin
				max = @tier3materialmax
			}
		}
	}
	option = {
		name = mem_anomalies.1.c
		from = {
			add_deposit = d_vast_unity_deposit
		}
		leader = {
			if = {
				limit = {
					has_trait = trait_resilient
				}
				add_skill_without_trait_selection = 1
			}
			else = {
				add_trait = leader_trait_resilient
			}
		}
	}
}
