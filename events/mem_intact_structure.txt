#An Intact Structure
#Idea by ViolentBeetle
#Code by Alex Lambson (alexwiwa)

namespace = mem_intact_structure

# Anomaly survey success art gallery 
ship_event = {
	id = mem_intact_structure.art
	title = "mem_intact_structure.1.name"
	desc = "mem_intact_structure.1.desc"
	picture = "GFX_evt_intact_structure"
	is_triggered_only = yes
	
	#Art
	option = {
		trigger = { 
			owner = {  
				NOR { 
					has_ethic = ethic_xenophobe  
					has_ethic = ethic_fanatic_xenophobe 
				} 
			} 
		}
		name = mem_intact_structure.1.default
		owner = {
			add_influence = 50
		}
		leader = {
			add_experience = 50
		}
	}
	
	option = {
		trigger = { 
			owner = {  
				OR { 
					has_ethic = ethic_xenophobe  
					has_ethic = ethic_fanatic_xenophobe 
				} 
			} 
		}
		name = mem_intact_structure.1.xenophobe
		owner = {
			add_influence = 50
		}
		leader = {
			add_experience = 50
		}
	}
}

# Anomaly Survey Success Military factory
ship_event = {
	
}

# Anomaly Survey Success Nuclear Waste Storage
ship_event = {

}

# Anomaly Survey Success Residential Building

# Anomaly survey failure
ship_event = {
	id = mem_intact_structure.failure
	title = "mem_intact_structure.failure.name"
	desc = "mem_intact_structure.failure.desc"
	picture = "GFX_evt_city_ruins"
	is_triggered_only = yes

	option = {
		name = mem_intact_structure.failure.response
	}
	
}

#Anomaly Critical Failure
ship_event = {
	if = mem_intact_structure.critical_failure
	title = mem_intact_structure.critical_failure.name
	desc = mem_intact_structure.critical_failure.desc
	picture = GFX_evt_city_ruins
	is_triggered_only = yes
	
	option = {
		name = mem_intact_structure.critical_failure.response
	}
}