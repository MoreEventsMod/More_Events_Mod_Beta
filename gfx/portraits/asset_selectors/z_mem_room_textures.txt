room_selector = {
	# Script may point to textures in 'gfx\portraits\city_sets\' that end with "_room.dds" while ignoring the file ending, e.g.: "room = machine_room" will find 'machine_room(.dds)'.

	game_setup = {
		#"mem_orila_2_room"								= { always = yes }
		"mem_orila_3_room"								= { always = yes }
		"mem_vazuran_2_room"							= { always = yes }	
	}

	ruler = {
		"mem_surveyor_green_room" = {
			exists = owner
			owner = {
				is_country_type = mem_surveyor_country_green
				NOT = {
					has_country_flag = mem_surveyor_country_green_sentinel_decision
				}
			}
		}
		"mem_surveyor_yellow_room" = {
			exists = owner
			owner = {
				OR = {
					AND = {
						is_country_type = mem_surveyor_country_green
						has_country_flag = mem_surveyor_country_green_sentinel_decision
					}
					has_ai_personality = mem_surveyor_country_yellow_personality
				}
				
			}
		}
	}
}